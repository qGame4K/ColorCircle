Генератор цветовых палитр

Веб‑приложение на Vue 3 + Vite для генерации цветовых палитр и работы с цветовым кругом.

---

## 1. Клонирование проекта с GitHub

1. Откройте терминал (PowerShell / cmd / bash).
2. Перейдите в папку, где хотите разместить проект:
   ```bash
   cd /путь/к/папке
   ```
3. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Mikle-kekich/KR5-Front-Back
   ```
4. Перейдите в папку проекта:
   ```bash
   cd REPO_NAME
   ```

---

## 2. Установка зависимостей

1. Убедитесь, что установлен Node.js (рекомендуется LTS‑версия).
2. В папке проекта выполните:
   ```bash
   npm install
   ```
3. Если появятся конфликты peer‑зависимостей, используйте:
   ```bash
   npm install --legacy-peer-deps
   ```

---

## 3. Конфигурация Vite (проверка)

В файле `vite.config.js` должно быть минимальное содержимое:

```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173
  }
})
```

Если раньше подключался `vite-plugin-vue-devtools`, удалите его из импортов и из массива `plugins`.

---

## 4. Запуск проекта в режиме разработки

1. В терминале в папке проекта выполните:
   ```bash
   npm run dev
   ```
2. После запуска откройте в браузере:
   ```text
   http://localhost:5173
   ```
3. Должна открыться страница с двумя вкладками:
   - Генератор палитр» — генерация случайных палитр
   - Цветовой круг» — выбор цвета по кругу и цветовые схемы

---

## 5. Функционал приложения

### Вкладка «Генератор палитр»

- **Генерация палитр** — нажмите кнопку «🎨 ГЕНЕРИРОВАТЬ ПАЛИТРУ»
- **Выбор количества цветов** — от 2 до 20 цветов
- **Выбор формата** — HEX, RGB или HSL
- **Закрепление цветов** — нажмите 📌 для закрепления, чтобы цвет не менялся при следующей генерации
- **Копирование цветов** — нажмите 📋 для копирования значения в буфер обмена
- **Сохранение палитр** — сохраняйте созданные палитры локально (до 10 штук)
- **Загрузка/удаление** — выбирайте из сохранённых палитр или удаляйте их

### Вкладка «Цветовой круг»

- **Интерактивный круг** — кликните на круг для выбора цвета
- **Цветовые схемы** — выбирайте тип схемы:
  - Комплементарная (противоположные цвета)
  - Аналогичная (соседние цвета)
  - Триадная (3 цвета на 120°)
  - Тетрадная (4 цвета на 90°)
  - Расщеплённо-комплементарная
  - Монохромная (5 оттенков одного цвета)
- **HUE слайдер** — изменяйте оттенок (0–360°)
- **Экспорт** — сохраняйте палитру в CSS переменные или JSON
- **Копирование** — копируйте цвета в буфер обмена


---

### Пустая «Текущая палитра» или ошибки в консоли

**Проверьте:**

1. Что все файлы в `src/components/` на месте и не пусты
2. Что все утилиты в `src/utils/` на месте
3. Что импорты указывают на правильные пути
4. Откройте F12 → Console и посмотрите красные ошибки
5. Перезагрузите страницу (F5) и проверьте опять

---



## Recommended IDE Setup

[VS Code](https://code.visualstudio.com/) + [Vue (Official)](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (and disable Vetur).

## Recommended Browser Setup

- Chromium-based browsers (Chrome, Edge, Brave, etc.):
  - [Vue.js devtools](https://chromewebstore.google.com/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd) 
  - [Turn on Custom Object Formatter in Chrome DevTools](http://bit.ly/object-formatters)
- Firefox:
  - [Vue.js devtools](https://addons.mozilla.org/en-US/firefox/addon/vue-js-devtools/)
  - [Turn on Custom Object Formatter in Firefox DevTools](https://fxdx.dev/firefox-devtools-custom-object-formatters/)

## Customize configuration

See [Vite Configuration Reference](https://vite.dev/config/).

## Project Setup

```sh
npm install
```

### Compile and Hot-Reload for Development

```sh
npm run dev
```

### Compile and Minify for Production

```sh
npm run build
```
